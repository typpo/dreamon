{
  "name": "keepdream-worker",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "mongodb": "^7.2.0",
    "postal-mime": "^2.7.4"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "version": "1.0.0",
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
